diff --git a/aQute/bnd/annotation/metatype/Configurable.java b/aQute/bnd/annotation/metatype/Configurable.java
index 6742c39..cd37c07 100644
--- a/aQute/bnd/annotation/metatype/Configurable.java
+++ b/aQute/bnd/annotation/metatype/Configurable.java
@@ -121,8 +121,13 @@ public class Configurable<T> {
 			}

 			Class<?> actualType = o.getClass();
-			if (actualType.isAssignableFrom(resultType))
+			if (actualType.isAssignableFrom(resultType)) {
+				if (resultType == String.class) {
+					return unescape((String)o);
+				}
+
 				return o;
+			}

 			if (resultType == boolean.class || resultType == Boolean.class) {
 				if (actualType == boolean.class || actualType == Boolean.class)
@@ -326,7 +331,7 @@ public class Configurable<T> {
 					.find())
 					return Arrays.asList(s.split("\\|"));
 				else
-					return unescape(s);
+					return Arrays.asList(s.split("(?<!\\\\),"));

 			}
 			return Arrays.asList(o);
@@ -358,19 +363,12 @@ public class Configurable<T> {
 		return sb.toString();
 	}

-	public static List<String> unescape(String s) {
+	public static String unescape(String s) {
 		// do it the OSGi way
-		List<String> tokens = new ArrayList<>();
+		s = s.replaceAll("^\\s*", "");
+		s = s.replaceAll("(?!<\\\\)\\s*$", "");

-		String[] parts = s.split("(?<!\\\\),");
-
-		for (String p : parts) {
-			p = p.replaceAll("^\\s*", "");
-			p = p.replaceAll("(?!<\\\\)\\s*$", "");
-			p = p.replaceAll("\\\\([\\s,\\\\|])", "$1");
-			tokens.add(p);
-		}
-		return tokens;
+		return s.replaceAll("\\\\([\\s,=\\\\|])", "$1");
 	}

 	private static final MethodType defaultConstructor = methodType(void.class);
@@ -400,3 +398,4 @@ public class Configurable<T> {
 	}

 }
+/* @generated */
\ No newline at end of file