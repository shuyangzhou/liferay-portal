diff --git a/org/phidias/compile/BundleJavaManager.java b/org/phidias/compile/BundleJavaManager.java
index 46f8d47..e42a523 100644
--- a/org/phidias/compile/BundleJavaManager.java
+++ b/org/phidias/compile/BundleJavaManager.java
@@ -1,3 +1,4 @@
+/* @generated */
 /**
  * Copyright 2012 Liferay Inc.
  *
@@ -77,9 +78,11 @@ public class BundleJavaManager
 
 		_strict = strict;
 
-		_log.log(
-			"Initializing compilation in OSGi for bundle " +
-				_bundle.getSymbolicName() + "-" + _bundle.getVersion());
+		if (_log.isEnabled()) {
+			_log.log(
+				"Initializing compilation in OSGi for bundle " +
+					_bundle.getSymbolicName() + "-" + _bundle.getVersion());
+		}
 
 		_bundleWiring = _bundle.adapt(BundleWiring.class);
 
@@ -90,9 +93,11 @@ public class BundleJavaManager
 
 		List<BundleWire> providedWires = _bundleWiring.getRequiredWires(null);
 
-		_log.log(
-			"Dependent BundleWirings included in this BundleJavaManager " +
-				"context: ");
+		if (_log.isEnabled()) {
+			_log.log(
+				"Dependent BundleWirings included in this BundleJavaManager " +
+					"context: ");
+		}
 
 		_bundleWirings = new ArrayList<BundleWiring>();
 
@@ -109,9 +114,11 @@ public class BundleJavaManager
 				_systemBundleWiring = providerWiring;
 			}
 
-			_log.log(
-				"\t" + curBundle.getSymbolicName() + "-" +
-					curBundle.getVersion());
+			if (_log.isEnabled()) {
+				_log.log(
+					"\t" + curBundle.getSymbolicName() + "-" +
+						curBundle.getVersion());
+			}
 
 			_bundleWirings.add(providerWiring);
 		}
@@ -159,7 +166,9 @@ public class BundleJavaManager
 			BundleJavaFileObject bundleJavaFileObject =
 				(BundleJavaFileObject)file;
 
-			_log.log("Infering binary name from " + bundleJavaFileObject);
+			if (_log.isEnabled()) {
+				_log.log("Infering binary name from " + bundleJavaFileObject);
+			}
 
 			return bundleJavaFileObject.inferBinaryName();
 		}
@@ -176,10 +185,21 @@ public class BundleJavaManager
 		List<JavaFileObject> javaFileObjects = new ArrayList<JavaFileObject>();
 
 		if (location == StandardLocation.CLASS_PATH) {
-			_log.log(
-				"List available sources for {location=" + location +
-					", packageName=" + packageName + ", kinds=" + kinds +
-						", recurse=" + recurse + "}");
+			if (_log.isEnabled()) {
+				StringBuilder sb = new StringBuilder(64);
+
+				sb.append("List available sources for {location=");
+				sb.append(location);
+				sb.append(", packageName=");
+				sb.append(packageName);
+				sb.append(", kinds=");
+				sb.append(kinds);
+				sb.append(", recurse=");
+				sb.append(recurse);
+				sb.append("}");
+
+				_log.log(sb.toString());
+			}
 		}
 
 		String packagePath = packageName.replace('.', '/');
@@ -207,7 +227,9 @@ public class BundleJavaManager
 
 			for (JavaFileObject javaFileObject : localJavaFileObjects) {
 				if (location == StandardLocation.CLASS_PATH) {
-					_log.log("\t" + javaFileObject);
+					if (_log.isEnabled()) {
+						_log.log("\t" + javaFileObject);
+					}
 				}
 
 				javaFileObjects.add(javaFileObject);
@@ -222,7 +244,12 @@ public class BundleJavaManager
 	}
 
 	private String getClassNameFromPath(String resourceName) {
-		return resourceName.replace(".class", "").replace("/", ".");
+		if (resourceName.endsWith(".class")) {
+			resourceName = resourceName.substring(
+				0, resourceName.length() - ".class".length());
+		}
+
+		return resourceName.replace('/', '.');
 	}
 
 	private URI getURI(URL url) {
@@ -233,7 +260,9 @@ public class BundleJavaManager
 				return url.toURI();
 			}
 			catch (Exception e) {
-				_log.log(e);
+				if (_log.isEnabled()) {
+					_log.log(e);
+				}
 			}
 		}
 		else if (protocol.equals("jar")) {
@@ -244,7 +273,9 @@ public class BundleJavaManager
 				return jarUrlConnection.getJarFileURL().toURI();
 			}
 			catch (Exception e) {
-				_log.log(e);
+				if (_log.isEnabled()) {
+					_log.log(e);
+				}
 			}
 		}
 		else if (protocol.equals("vfs")) {
@@ -253,8 +284,8 @@ public class BundleJavaManager
 			int indexOf = file.indexOf(".jar") + 4;
 
 			file =
-				file.substring(0, indexOf) + "!" +
-					url.getFile().substring(indexOf, url.getFile().length());
+				file.substring(0, indexOf).concat("!").concat(
+					file.substring(indexOf, file.length()));
 
 			return new File(file).toURI();
 		}
@@ -276,7 +307,9 @@ public class BundleJavaManager
 				return new BundleJavaFileObject(uri, className);
 			}
 			catch (Exception e) {
-				_log.log(e);
+				if (_log.isEnabled()) {
+					_log.log(e);
+				}
 			}
 		}
 		else if (protocol.equals("jar")) {
@@ -291,7 +324,9 @@ public class BundleJavaManager
 					resourceName);
 			}
 			catch (IOException e) {
-				_log.log(e);
+				if (_log.isEnabled()) {
+					_log.log(e);
+				}
 			}
 		}
 
@@ -361,14 +396,18 @@ public class BundleJavaManager
 				resourceURL, resourceName);
 
 			if (javaFileObject == null) {
-				_log.log(
-					"\tCould not create JavaFileObject for {" + resourceURL +
-						"}");
+				if (_log.isEnabled()) {
+					_log.log(
+						"\tCould not create JavaFileObject for {" +
+							resourceURL + "}");
+				}
 
 				continue;
 			}
 
-			_log.log("\t" + javaFileObject);
+			if (_log.isEnabled()) {
+				_log.log("\t" + javaFileObject);
+			}
 
 			javaFileObjects.add(javaFileObject);
 		}
diff --git a/org/phidias/compile/ClassLoaderJavaManager.java b/org/phidias/compile/ClassLoaderJavaManager.java
index e784eef..d9b5118 100644
--- a/org/phidias/compile/ClassLoaderJavaManager.java
+++ b/org/phidias/compile/ClassLoaderJavaManager.java
@@ -1,3 +1,4 @@
+/* @generated */
 /**
  * Copyright 2012 Liferay Inc.
  *
@@ -125,7 +126,9 @@ public class ClassLoaderJavaManager
 			BundleJavaFileObject bundleJavaFileObject =
 				(BundleJavaFileObject)file;
 
-			_log.log("Infering binary name from " + bundleJavaFileObject);
+			if (_log.isEnabled()) {
+				_log.log("Infering binary name from " + bundleJavaFileObject);
+			}
 
 			return bundleJavaFileObject.inferBinaryName();
 		}
@@ -142,10 +145,21 @@ public class ClassLoaderJavaManager
 		List<JavaFileObject> javaFileObjects = new ArrayList<JavaFileObject>();
 
 		if (location == StandardLocation.CLASS_PATH) {
-			_log.log(
-				"List available sources for {location=" + location +
-					", packageName=" + packageName + ", kinds=" + kinds +
-						", recurse=" + recurse + "}");
+			if (_log.isEnabled()) {
+				StringBuilder sb = new StringBuilder(64);
+
+				sb.append("List available sources for {location=");
+				sb.append(location);
+				sb.append(", packageName=");
+				sb.append(packageName);
+				sb.append(", kinds=");
+				sb.append(kinds);
+				sb.append(", recurse=");
+				sb.append(recurse);
+				sb.append("}");
+
+				_log.log(sb.toString());
+			}
 		}
 
 		String packagePath = packageName.replace('.', '/');
diff --git a/org/phidias/compile/TPhLog.java b/org/phidias/compile/TPhLog.java
index d3c8d53..4fc4ef6 100644
--- a/org/phidias/compile/TPhLog.java
+++ b/org/phidias/compile/TPhLog.java
@@ -1,3 +1,4 @@
+/* @generated */
 /**
  * Copyright 2012 Liferay Inc.
  *
@@ -30,8 +31,12 @@ public class TPhLog {
 	public PrintStream out;
 	public String pattern = "%1$tH:%1$tM:%1$tS,%1$tL [%2$s:%3$d] %4$s %5$s\n";
 
+	public boolean isEnabled() {
+		return (out != null);
+	}
+
 	public void log(Object... input) {
-		if (out == null) {
+		if (!isEnabled()) {
 			return;
 		}
 
